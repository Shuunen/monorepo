{
  "$schema": "./node_modules/nx/schemas/project-schema.json",
  "targets": {
    "oxlint": {
      "cache": false,
      "dependsOn": [{ "projects": ["*"], "target": "generate-*" }],
      "executor": "nx:run-commands",
      "options": {
        "command": "oxlint --type-aware --deny-warnings --report-unused-disable-directives",
        "cwd": "{projectRoot}"
      }
    },
    "oxlint-fix": {
      "cache": false,
      "dependsOn": [{ "projects": ["*"], "target": "generate-*" }],
      "executor": "nx:run-commands",
      "options": {
        "command": "oxlint --fix --fix-suggestions --fix-dangerously --type-aware --deny-warnings --report-unused-disable-directives",
        "cwd": "{projectRoot}"
      }
    },
    "oxfmt": {
      "cache": false,
      "executor": "nx:run-commands",
      "options": {
        "command": "oxfmt --check",
        "cwd": "{projectRoot}"
      }
    },
    "oxfmt-fix": {
      "cache": false,
      "executor": "nx:run-commands",
      "options": {
        "command": "oxfmt --write",
        "cwd": "{projectRoot}"
      }
    },
    "sherif": {
      "cache": false,
      "executor": "nx:run-commands",
      "options": {
        "command": "sherif",
        "cwd": "{projectRoot}"
      }
    },
    "sherif-fix": {
      "cache": false,
      "executor": "nx:run-commands",
      "options": {
        "command": "sherif --fix",
        "cwd": "{projectRoot}"
      }
    },
    "headers": {
      "cache": false,
      "executor": "nx:run-commands",
      "options": {
        "command": "bun libs/utils/src/bin/header-injector.cli.ts --header=\"Copyright 2026 ACME\"",
        "cwd": "{projectRoot}"
      }
    },
    "global": {
      "cache": false,
      "dependsOn": ["oxlint", "oxfmt", "sherif"]
    },
    "global-fix": {
      "cache": false,
      "dependsOn": ["oxlint-fix", "oxfmt-fix", "sherif-fix"]
    }
  }
}
